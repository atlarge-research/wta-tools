unit-tests:
  stage: tests

  script:
    - echo "Unit tests started"
    - "mvn surefire:test"
#    - "mvn jacoco:check" todo, fail when <80%


integration-tests:
  stage: tests

  script:
    - echo "Integration tests started"
    - "mvn failsafe:integration-test"
#    - todo, fail when <80%


mutation-tests:
  stage: tests
  allow_failure: true

  script:
    - echo "Mutation tests started"
    - "mvn pitest:mutationCoverage"
#    - todo, fail when <60%
