unit-tests:
  stage: tests

  script:
    - echo "Unit tests started"
    - "mvn surefire:test"
#    - "mvn jacoco:check" todo, fail when <80%


integration-tests:
  stage: tests

  script:
    - echo "Integration tests started"
    - "mvn failsafe:integration-tests"
#    - todo, fail when <80%


mutation-tests:
  stage: tests

  script:
    - echo "Mutation tests started"
    - "mvn pitest:mutationCoverage"
#    - todo, fail when <60%
