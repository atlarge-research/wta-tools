build-core:
  stage: build-core

  script:
    - echo "Build core started"
    - "mvn -pl core test-compile compile"

  artifacts:
    build:
      core:
        expire_in: 2 hours
        paths:
          - core/target/
